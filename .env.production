# Public build-time values — inlined into client JS by `next build`.
# Cloudflare's CI does not pass dashboard "Variables and Secrets" into the
# build step (only into the deployed Worker's runtime), so these must live
# here to actually reach the browser bundle. Safe to commit: NEXT_PUBLIC_*
# values are exposed to the client regardless.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAEUjgVwMgq6ynA3G
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-V3SZXC5409
